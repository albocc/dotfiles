# Set referrer automatically when following redirects
referer = ";auto"

# Pretend to be Internet Explorer 9 on Windows 7
user-agent = "Mozilla/5.0 (compatible; MSIE 9.0; Windows NT 6.1; Trident/5.0)"

# Set timeout to a minute
connect-timeout = 60

